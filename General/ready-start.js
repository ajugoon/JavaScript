// program to start the user interaction
// initialize the variable
let ready = 'no';

// while loop will iterate until 'yes' is the response given
while (ready != 'yes') {
  console.log('Welcome!!!');
  ready = String(prompt('Do you want to start the program?'));
}
console.log('Ok, NOW we are ready to roll!')
